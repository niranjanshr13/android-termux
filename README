# Termux Installation
```
install f-droid
install termux
install termux:tasker
remove f-droid
```

GIT_REPO_URL='git@github.com:niranjanshr13/android-termux'
GIT_REPO_PATH="$HOME/android-termux"
git clone git@github.com:niranjanshr13/android-termux $HOME/android-termux

cat "source $HOME/android-termux/config/bashrc" > ~/.bashrc