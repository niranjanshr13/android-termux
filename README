# Termux Installation
```
install f-droid
install termux
install termux:tasker
remove f-droid
```

# initial command
```
pkg install git -y
GIT_REPO_URL="https://github.com/niranjanshr13/android-termux"
git clone $GIT_REPO_URL
echo "source $HOME/android-termux/config/bashrc" > ~/.bashrc
```

# things to do
- adb connect from termux to the devices
- a flask app that send command to adb